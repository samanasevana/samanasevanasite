---
import { getCollection } from "astro:content";
import { SITE_DESCRIPTION, SITE_TITLE } from "../consts";
import Layout from "../layouts/Layout.astro";

import EventsTable from "../components/EventsTable.astro";
import { Content } from "../content/pages/index.md";

const homeSections = (await getCollection("homeSections")).sort(
  (a, b) => a.data.order - b.data.order,
);

const toneClasses = {
  emerald: "bg-emerald-50 text-emerald-700 ring-emerald-500/30",
  amber: "bg-amber-50 text-amber-700 ring-amber-400/30",
  indigo: "bg-indigo-50 text-indigo-700 ring-indigo-500/30",
  zinc: "bg-zinc-100 text-zinc-700 ring-zinc-400/30",
} as const;
---

<Layout title={SITE_TITLE} description={SITE_DESCRIPTION}>
  <main class="space-y-6">
    <div class="flex items-center gap-2">
      <img
        src="/static/bhante-1.png"
        alt="Venerable Jeffrey"
        class="h-36 w-full rounded-md border border-zinc-500/50 object-cover"
      />
      <img
        src="/static/bhante-2.png"
        alt="Leaf"
        class="h-36 w-full rounded-md border border-zinc-500/50 object-cover"
      />
    </div>
    <article class="space-y-6 text-base text-slate-700">
      <div class="prose prose-slate max-w-none text-lg text-slate-900">
        <Content />
      </div>
      <div class="space-y-6">
        <div class="lg:mx-[-100px]">
          <EventsTable />
        </div>
        <div
          class="rounded-2xl border border-slate-200 bg-white p-5 shadow-md shadow-slate-200/70"
        >
          <div class="space-y-5 text-sm text-slate-700">
            {
              homeSections.map(async (section, index) => {
                const { Content: SectionContent } = await section.render();
                return (
                  <>
                    <div>
                      <div class="mb-2 flex flex-wrap items-center gap-2">
                        <span
                          class={`inline-flex items-center rounded-full px-3 py-1 text-xs font-semibold uppercase tracking-wide ring-1 ring-inset ${
                            toneClasses[section.data.tone]
                          }`}
                        >
                          {section.data.badge}
                        </span>
                        {section.data.meta ? (
                          <span class="text-xs font-medium uppercase tracking-wide text-slate-500">
                            {section.data.meta}
                          </span>
                        ) : null}
                      </div>
                      <div class="prose prose-slate max-w-none text-base text-slate-700">
                        <SectionContent />
                      </div>
                    </div>
                    {index < homeSections.length - 1 ? (
                      <hr class="border-slate-200" />
                    ) : null}
                  </>
                );
              })
            }
          </div>
        </div>
      </div>
    </article>
  </main>
</Layout>
